# クリーチャービジュアル v2 実装計画（Issue #6）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 承認済みモックアップの v2 クリーチャー生成（全5種族）を TypeScript に移植し、`src/logic/creatures/` として現行 `creatureGenerator.ts` を置き換える。

**Architecture:** パーツライブラリ合成＋一筆シルエット方式。共通ヘルパー（palette/parts）＋種族別ジェネレーター5ファイル＋公開API（index）に分割。`creatureGenerator.ts` は再エクスポートに変えて既存 import と既存テストを壊さない。

**Tech Stack:** TypeScript / Vite / Vitest（既存構成のまま。依存追加なし）

## Global Constraints

- 設計スペック: `docs/superpowers/specs/2026-07-07-creature-v2-design.md`
- **移植元（唯一の真実）**: `docs/superpowers/specs/2026-07-07-creature-v2-mockup.html` 内の
  `colors2` / `glossyEye` / `browPair` / `slimeV2` / `beastV2` / `bipedV2` / `eyeTentacleV2` / `orbV2`。
  ジオメトリ・パーツ選択式は**逐語移植**（見た目はユーザー承認済みのため変更禁止）
- 移植時の機械的変換ルール（全タスク共通）:
  1. `dna` 引数に `KanjiDNA` 型（`src/types/game.ts`）を付ける
  2. `f` / `colors2` / `glossyEye` / `browPair` / `nextClipId` / `wrapCreatureSvg` は `palette.ts` / `parts.ts` から import
  3. モックアップの `wrap2`（width150）と手書き `<svg width="150"...>` はすべて `wrapCreatureSvg(inner, mirror)`（**width/height 120**）に置き換える
  4. `'v2s' + uid++` 等の clipPath id は `nextClipId(接頭辞)` に置き換える
  5. 日本語コメントは維持する
- 公開 API（`generateCreature` / `selectSpecies` / `generateCreatureName`）のシグネチャ・種族割り当て・名前生成は変更しない
- 決定性: 乱数・Date を使わない。決定性テストは clipPath id を正規化して比較する
- 作業ブランチ: `feature-creature-v2`（main から作成）
- 各タスク完了時に `npx vitest run` が全件 PASS であること

---

### Task 1: 共通モジュール（palette / parts）

**Files:**
- Create: `src/logic/creatures/palette.ts`
- Create: `src/logic/creatures/parts.ts`
- Test: `src/__tests__/logic/creatures/parts.test.ts`

**Interfaces:**
- Produces:
  - `palette.ts`: `creatureColors(hue: number): CreaturePalette`（`{ main, belly, dark, outline, accent }` すべて string）
  - `parts.ts`: `f(n: number): string` / `glossyEye(ex, ey, r, c): string` / `browPair(lx, rx, y, w, c): string` / `nextClipId(prefix: string): string` / `wrapCreatureSvg(inner: string, mirror: boolean): string` / `CHEEK_COLOR: string`

- [ ] **Step 0: ブランチ作成**

```bash
git checkout main && git checkout -b feature-creature-v2
```

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/parts.test.ts
import { describe, it, expect } from 'vitest'
import { creatureColors } from '../../../logic/creatures/palette'
import { f, glossyEye, browPair, nextClipId, wrapCreatureSvg } from '../../../logic/creatures/parts'

describe('creatureColors', () => {
  it('hue から5色のパレットを返す', () => {
    const c = creatureColors(120)
    expect(c.main).toBe('hsl(120,68%,55%)')
    expect(c.belly).toBe('hsl(120,60%,76%)')
    expect(c.dark).toBe('hsl(120,60%,36%)')
    expect(c.outline).toBe('hsl(120,55%,15%)')
    expect(c.accent).toBe('hsl(300,75%,60%)')
  })
})

describe('parts ヘルパー', () => {
  it('f は小数1桁に丸める', () => {
    expect(f(1.2345)).toBe('1.2')
  })

  it('glossyEye は白目・瞳・ハイライト2つを含む', () => {
    const c = creatureColors(0)
    const eye = glossyEye(50, 40, 5, c)
    expect(eye).toContain('fill="white"')
    expect(eye).toContain(c.outline)
    expect((eye.match(/<circle/g) ?? []).length).toBeGreaterThanOrEqual(3)
  })

  it('browPair は内側が下がる2本の線を返す', () => {
    const c = creatureColors(0)
    expect((browPair(40, 70, 30, 5, c).match(/<line/g) ?? []).length).toBe(2)
  })

  it('nextClipId は呼ぶたびに異なる id を返す', () => {
    expect(nextClipId('s')).not.toBe(nextClipId('s'))
  })

  it('wrapCreatureSvg は 120x120 の svg で包み、mirror 指定で反転 g を挟む', () => {
    const plain = wrapCreatureSvg('<circle/>', false)
    expect(plain).toMatch(/^<svg width="120" height="120" viewBox="0 0 120 120">/)
    expect(plain).not.toContain('scale(-1,1)')
    expect(wrapCreatureSvg('<circle/>', true)).toContain('translate(120,0) scale(-1,1)')
  })
})
```

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/parts.test.ts`
Expected: FAIL（モジュール未作成）

- [ ] **Step 3: 実装**

```ts
// src/logic/creatures/palette.ts
// クリーチャー v2 共通パレット。hue は漢字DNA由来
export interface CreaturePalette {
  main: string
  belly: string
  dark: string
  outline: string
  accent: string
}

export function creatureColors(hue: number): CreaturePalette {
  return {
    main: `hsl(${hue},68%,55%)`,
    belly: `hsl(${hue},60%,76%)`,
    dark: `hsl(${hue},60%,36%)`,
    outline: `hsl(${hue},55%,15%)`,
    accent: `hsl(${(hue + 180) % 360},75%,60%)`,
  }
}
```

```ts
// src/logic/creatures/parts.ts
// 全種族共通の描画パーツ（設計: docs/superpowers/specs/2026-07-07-creature-v2-design.md）
import type { CreaturePalette } from './palette'

export const CHEEK_COLOR = 'hsl(350,85%,72%)'

export const f = (n: number): string => n.toFixed(1)

let uid = 0
/** clipPath 用の一意 id。id は毎回変わるため決定性比較時は正規化すること */
export function nextClipId(prefix: string): string {
  return `${prefix}${uid++}`
}

export function wrapCreatureSvg(inner: string, mirror: boolean): string {
  const content = mirror
    ? `<g transform="translate(120,0) scale(-1,1)">${inner}</g>`
    : inner
  return `<svg width="120" height="120" viewBox="0 0 120 120">${content}</svg>`
}

// 大きくつややかな目（かわいらしさの核）
export function glossyEye(ex: number, ey: number, r: number, c: CreaturePalette): string {
  const out: string[] = []
  out.push(`<ellipse cx="${f(ex)}" cy="${f(ey)}" rx="${f(r * 0.85)}" ry="${f(r)}" fill="white" stroke="${c.outline}" stroke-width="1.6"/>`)
  out.push(`<circle cx="${f(ex)}" cy="${f(ey + r * 0.18)}" r="${f(r * 0.5)}" fill="${c.outline}"/>`)
  out.push(`<circle cx="${f(ex + r * 0.2)}" cy="${f(ey - r * 0.05)}" r="${f(r * 0.19)}" fill="white"/>`)
  out.push(`<circle cx="${f(ex - r * 0.14)}" cy="${f(ey + r * 0.36)}" r="${f(r * 0.09)}" fill="white" opacity="0.9"/>`)
  return out.join('')
}

// 内側が下がる怒り眉（left: 右下がり / right: 左下がり）
export function browPair(lx: number, rx: number, y: number, w: number, c: CreaturePalette): string {
  return (
    `<line x1="${f(lx - w)}" y1="${f(y - w * 0.5)}" x2="${f(lx + w)}" y2="${f(y + w * 0.4)}" stroke="${c.outline}" stroke-width="2.6" stroke-linecap="round"/>` +
    `<line x1="${f(rx - w)}" y1="${f(y + w * 0.4)}" x2="${f(rx + w)}" y2="${f(y - w * 0.5)}" stroke="${c.outline}" stroke-width="2.6" stroke-linecap="round"/>`
  )
}
```

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/parts.test.ts`
Expected: PASS（6件）

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures src/__tests__/logic/creatures
git commit -m "feat: add creature v2 shared palette and parts helpers (#6)"
```

---

### 共通: 種族タスク（Task 2〜6）のテンプレート

Task 2〜6 は同じ手順で種族を1つずつ移植する。各タスクで:

1. モックアップ `docs/superpowers/specs/2026-07-07-creature-v2-mockup.html` から対象関数を
   **逐語**コピーし、Global Constraints の変換ルール 1〜5 を適用して
   `src/logic/creatures/<species>.ts` を作る
2. 関数名は `generate<Species>V2` → export 名は `generate<Species>`（index から利用）
3. モックアップ内でしか使っていないローカルヘルパー（例: slime の `shadow`、
   beast の `legShape`）は、その種族ファイル内に移す（他種族と共有しない限り parts に置かない）
4. テストは下記共通形式＋種族固有のバリアント検証

決定性テスト用の正規化ヘルパー（各テストファイルにコピーせず、Task 2 で
`src/__tests__/logic/creatures/testUtils.ts` として1回だけ作る）:

```ts
// src/__tests__/logic/creatures/testUtils.ts
// clipPath id は生成ごとに変わるため、決定性比較の前に正規化する
export function normalizeIds(svg: string): string {
  return svg.replace(/id="[^"]+"/g, 'id="X"').replace(/url\(#[^)]+\)/g, 'url(#X)')
}
```

---

### Task 2: スライム

**Files:**
- Create: `src/logic/creatures/slime.ts`
- Create: `src/__tests__/logic/creatures/testUtils.ts`（上記コード）
- Test: `src/__tests__/logic/creatures/slime.test.ts`

**Interfaces:**
- Consumes: Task 1 の `creatureColors` / `f` / `glossyEye` / `browPair` / `nextClipId` / `wrapCreatureSvg` / `CHEEK_COLOR`
- Produces: `generateSlime(dna: KanjiDNA): string`（SVG 文字列）

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/slime.test.ts
import { describe, it, expect } from 'vitest'
import { generateSlime } from '../../../logic/creatures/slime'
import { normalizeIds } from './testUtils'
import type { KanjiDNA } from '../../../types/game'

const base: KanjiDNA = { strokeCount: 5, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 120 }

describe('generateSlime', () => {
  it('有効な 120x120 SVG を返す', () => {
    const svg = generateSlime(base)
    expect(svg).toMatch(/^<svg width="120" height="120"/)
    expect(svg).toContain('</svg>')
  })

  it('同じ DNA から同じ SVG（id 正規化後）を返す', () => {
    expect(normalizeIds(generateSlime(base))).toBe(normalizeIds(generateSlime(base)))
  })

  it('symmetry > 0.55 でほっぺ（にこにこ）、以下で怒り眉', () => {
    const cute = generateSlime({ ...base, symmetry: 0.8 })
    const angry = generateSlime({ ...base, symmetry: 0.3 })
    expect(cute).toContain('hsl(350,85%,72%)')
    expect(angry).not.toContain('hsl(350,85%,72%)')
  })

  it('hRatio で体型が変わる（とろけ型は水たまり用 ellipse が増える）', () => {
    const classic = generateSlime({ ...base, hRatio: 0.3 })
    const melty = generateSlime({ ...base, hRatio: 0.5 })
    expect(normalizeIds(classic)).not.toBe(normalizeIds(melty))
  })

  it('底面はフラット（波打ちの Q 連打がない）: 底辺に L コマンドを含む', () => {
    expect(generateSlime(base)).toMatch(/L[\d.]+,90 /)
  })
})
```

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/slime.test.ts`
Expected: FAIL（モジュール未作成）

- [ ] **Step 3: 実装（モックアップ `slimeV2` を移植）**

移植元: mockup HTML の `// ── スライム v2 ──` セクション全体（`slimeV2` 関数）。
変換ルール適用後の骨格（本体コードはモックアップから逐語コピー）:

```ts
// src/logic/creatures/slime.ts
import type { KanjiDNA } from '../../types/game'
import { creatureColors } from './palette'
import { f, glossyEye, browPair, nextClipId, wrapCreatureSvg, CHEEK_COLOR } from './parts'

// 一筆の玉ねぎ型シルエット。bodyVar: 0=クラシック 1=とろけ 2=とげ背
export function generateSlime(dna: KanjiDNA): string {
  // …mockup slimeV2 本体を逐語移植（wrap2 → wrapCreatureSvg(parts.join(''), false)、
  //   'v2s' + uid++ → nextClipId('slime')、ほっぺ色リテラル → CHEEK_COLOR）…
}
```

注意: スライムは向き反転がないので `wrapCreatureSvg(parts.join(''), false)`。

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/slime.test.ts`
Expected: PASS（5件）

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures/slime.ts src/__tests__/logic/creatures
git commit -m "feat: port slime v2 generator from approved mockup (#6)"
```

---

### Task 3: けもの

**Files:**
- Create: `src/logic/creatures/beast.ts`
- Test: `src/__tests__/logic/creatures/beast.test.ts`

**Interfaces:**
- Consumes: Task 1 のヘルパー、`testUtils.normalizeIds`
- Produces: `generateBeast(dna: KanjiDNA): string`

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/beast.test.ts
import { describe, it, expect } from 'vitest'
import { generateBeast } from '../../../logic/creatures/beast'
import { normalizeIds } from './testUtils'
import type { KanjiDNA } from '../../../types/game'

const base: KanjiDNA = { strokeCount: 5, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 30 }

describe('generateBeast', () => {
  it('有効な 120x120 SVG を返す', () => {
    const svg = generateBeast(base)
    expect(svg).toMatch(/^<svg width="120" height="120"/)
    expect(svg).toContain('</svg>')
  })

  it('同じ DNA から同じ SVG（id 正規化後）を返す', () => {
    expect(normalizeIds(generateBeast(base))).toBe(normalizeIds(generateBeast(base)))
  })

  it('向き反転が DNA で変わる', () => {
    // faceLeft = (sc + round(hR*10) + round(hue/30)) % 2 === 0
    const left = generateBeast({ ...base, strokeCount: 4 }) // 4+5+1=10 偶数 → 反転
    const right = generateBeast({ ...base, strokeCount: 5 }) // 11 奇数 → そのまま
    expect(left).toContain('scale(-1,1)')
    expect(right).not.toContain('scale(-1,1)')
  })

  it('スタンスが DNA で変わる（座りは胴体回転 rotate(-25 を含む）', () => {
    // stanceVar = (sc + round(cur*10) + round(sym*10)) % 3 → 1 で座り
    const sitting = generateBeast({ strokeCount: 5, hRatio: 0.5, curvature: 0.8, symmetry: 0.3, hue: 30 }) // 5+8+3=16 → 1
    const standing = generateBeast({ strokeCount: 4, hRatio: 0.5, curvature: 0.4, symmetry: 0.8, hue: 30 }) // 4+4+8=16 → 1? 計算し直し: 16%3=1 → 別値に
    expect(sitting).toContain('rotate(-25')
  })

  it('curvature > 0.5 でたてがみが付く（polygon 数が増える）', () => {
    const plain = generateBeast({ ...base, curvature: 0.2 })
    const maned = generateBeast({ ...base, curvature: 0.8 })
    expect((maned.match(/<polygon/g) ?? []).length).toBeGreaterThan(
      (plain.match(/<polygon/g) ?? []).length,
    )
  })
})
```

注意: Step 1 で書いた DNA 値のスタンス/向きの期待値は、実装前に
`(sc + round(cur*10) + round(sym*10)) % 3` / `(sc + round(hR*10) + round(hue/30)) % 2`
を手計算して**コメントの計算式と一致することを確認**する（上の standing 用 DNA は
16%3=1 になってしまうので `strokeCount: 3`（3+4+8=15 → 0=直立）に修正して使う）。

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/beast.test.ts`
Expected: FAIL

- [ ] **Step 3: 実装（モックアップ `beastV2` を移植）**

移植元: mockup HTML の `// ── けもの v2 ──` セクション。`legShape` ローカル関数ごと移植。
末尾は `wrapCreatureSvg(body, faceLeft)` に置き換える（`body` は head 回転 g 結合済み文字列）。

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/beast.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures/beast.ts src/__tests__/logic/creatures/beast.test.ts
git commit -m "feat: port beast v2 generator from approved mockup (#6)"
```

---

### Task 4: にそくほこう

**Files:**
- Create: `src/logic/creatures/biped.ts`
- Test: `src/__tests__/logic/creatures/biped.test.ts`

**Interfaces:**
- Consumes: Task 1 のヘルパー、`testUtils.normalizeIds`
- Produces: `generateBiped(dna: KanjiDNA): string`

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/biped.test.ts
import { describe, it, expect } from 'vitest'
import { generateBiped } from '../../../logic/creatures/biped'
import { normalizeIds } from './testUtils'
import type { KanjiDNA } from '../../../types/game'

const base: KanjiDNA = { strokeCount: 5, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 260 }

describe('generateBiped', () => {
  it('有効な 120x120 SVG を返す', () => {
    const svg = generateBiped(base)
    expect(svg).toMatch(/^<svg width="120" height="120"/)
    expect(svg).toContain('</svg>')
  })

  it('同じ DNA から同じ SVG（id 正規化後）を返す', () => {
    expect(normalizeIds(generateBiped(base))).toBe(normalizeIds(generateBiped(base)))
  })

  it('symmetry <= 0.55 で怒り眉・> 0.55 でほっぺ', () => {
    expect(generateBiped({ ...base, symmetry: 0.8 })).toContain('hsl(350,85%,72%)')
    expect(generateBiped({ ...base, symmetry: 0.3 })).not.toContain('hsl(350,85%,72%)')
  })

  it('ツノ/毛・ポーズが DNA で変わる', () => {
    const a = generateBiped({ strokeCount: 3, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 260 })
    const b = generateBiped({ strokeCount: 4, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 260 })
    expect(normalizeIds(a)).not.toBe(normalizeIds(b))
  })
})
```

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/biped.test.ts`
Expected: FAIL

- [ ] **Step 3: 実装（モックアップ `bipedV2` を移植）**

移植元: mockup HTML の `// ── 二足歩行 v2 ──` セクション。
`arm` / `fist` / `downL` / `downR` ローカル関数ごと移植。
末尾は `wrapCreatureSvg(parts.join(''), faceLeft)`。

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/biped.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures/biped.ts src/__tests__/logic/creatures/biped.test.ts
git commit -m "feat: port biped v2 generator from approved mockup (#6)"
```

---

### Task 5: しょくしゅがん

**Files:**
- Create: `src/logic/creatures/eyeTentacle.ts`
- Test: `src/__tests__/logic/creatures/eyeTentacle.test.ts`

**Interfaces:**
- Consumes: Task 1 のヘルパー、`testUtils.normalizeIds`
- Produces: `generateEyeTentacle(dna: KanjiDNA): string`

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/eyeTentacle.test.ts
import { describe, it, expect } from 'vitest'
import { generateEyeTentacle } from '../../../logic/creatures/eyeTentacle'
import { normalizeIds } from './testUtils'
import type { KanjiDNA } from '../../../types/game'

const base: KanjiDNA = { strokeCount: 5, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 0 }

describe('generateEyeTentacle', () => {
  it('有効な 120x120 SVG を返す', () => {
    const svg = generateEyeTentacle(base)
    expect(svg).toMatch(/^<svg width="120" height="120"/)
    expect(svg).toContain('</svg>')
  })

  it('同じ DNA から同じ SVG（id 正規化後）を返す', () => {
    expect(normalizeIds(generateEyeTentacle(base))).toBe(normalizeIds(generateEyeTentacle(base)))
  })

  it('symmetry > 0.55 でまる瞳（circle 瞳）・以下で縦スリット（ellipse 瞳）', () => {
    const round = generateEyeTentacle({ ...base, symmetry: 0.8 })
    const slit = generateEyeTentacle({ ...base, symmetry: 0.3 })
    expect(normalizeIds(round)).not.toBe(normalizeIds(slit))
  })

  it('触手の本数が strokeCount で変わる（4 + sc % 4 本）', () => {
    // 触手は先細り塗りパス（Z で閉じる path）。本数差が path 数に表れる
    const four = generateEyeTentacle({ ...base, strokeCount: 4 }) // 4本
    const seven = generateEyeTentacle({ ...base, strokeCount: 7 }) // 7本
    expect((seven.match(/<path/g) ?? []).length).toBe((four.match(/<path/g) ?? []).length + 3)
  })
})
```

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/eyeTentacle.test.ts`
Expected: FAIL

- [ ] **Step 3: 実装（モックアップ `eyeTentacleV2` を移植）**

移植元: mockup HTML の `// ── 触手眼 v2 ──` セクション。
向き反転はないので `wrapCreatureSvg(parts.join(''), false)`。

注意: まぶた（lidVar 1/2）も `<path>` を追加するため、Step 1 の触手本数テストは
lidVar が同じになる DNA ペアで比較している（sc4: (4+5)%3=0, sc7: (7+5)%3=0 → 両方ぱっちり）。
DNA 値を変える場合は lidVar / pupilVar が一致することを確認する。

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/eyeTentacle.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures/eyeTentacle.ts src/__tests__/logic/creatures/eyeTentacle.test.ts
git commit -m "feat: port eye-tentacle v2 generator from approved mockup (#6)"
```

---

### Task 6: ふゆうたい

**Files:**
- Create: `src/logic/creatures/orb.ts`
- Test: `src/__tests__/logic/creatures/orb.test.ts`

**Interfaces:**
- Consumes: Task 1 のヘルパー、`testUtils.normalizeIds`
- Produces: `generateOrb(dna: KanjiDNA): string`

- [ ] **Step 1: 失敗するテストを書く**

```ts
// src/__tests__/logic/creatures/orb.test.ts
import { describe, it, expect } from 'vitest'
import { generateOrb } from '../../../logic/creatures/orb'
import { normalizeIds } from './testUtils'
import type { KanjiDNA } from '../../../types/game'

const base: KanjiDNA = { strokeCount: 5, hRatio: 0.5, curvature: 0.3, symmetry: 0.8, hue: 200 }

describe('generateOrb', () => {
  it('有効な 120x120 SVG を返す', () => {
    const svg = generateOrb(base)
    expect(svg).toMatch(/^<svg width="120" height="120"/)
    expect(svg).toContain('</svg>')
  })

  it('同じ DNA から同じ SVG（id 正規化後）を返す', () => {
    expect(normalizeIds(generateOrb(base))).toBe(normalizeIds(generateOrb(base)))
  })

  it('オーラの破線円ときらめきを含む', () => {
    const svg = generateOrb(base)
    expect(svg).toContain('stroke-dasharray')
    expect(svg).toContain('rotate(45')
  })

  it('体シルエットが DNA で変わる（玉/おばけ/ほのお）', () => {
    // bodyVar = (sc + round(sym*10)) % 3
    const orb = generateOrb({ ...base, strokeCount: 4, symmetry: 0.8 }) // (4+8)%3=0 玉
    const ghost = generateOrb({ ...base, strokeCount: 5, symmetry: 0.8 }) // (5+8)%3=1 おばけ
    const flame = generateOrb({ ...base, strokeCount: 6, symmetry: 0.8 }) // (6+8)%3=2 ほのお
    expect(normalizeIds(orb)).not.toBe(normalizeIds(ghost))
    expect(normalizeIds(ghost)).not.toBe(normalizeIds(flame))
  })
})
```

- [ ] **Step 2: テスト失敗を確認**

Run: `npx vitest run src/__tests__/logic/creatures/orb.test.ts`
Expected: FAIL

- [ ] **Step 3: 実装（モックアップ `orbV2` を移植）**

移植元: mockup HTML の `// ── 浮遊体 v2 ──` セクション。
末尾の傾き g（`rotate(tilt)`）＋向き反転は
`wrapCreatureSvg(`<g transform="rotate(${f(tilt)} ${cx} ${cy})">${parts.join('')}</g>`, faceLeft)`。

- [ ] **Step 4: テスト成功を確認**

Run: `npx vitest run src/__tests__/logic/creatures/orb.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatures/orb.ts src/__tests__/logic/creatures/orb.test.ts
git commit -m "feat: port orb v2 generator from approved mockup (#6)"
```

---

### Task 7: 公開 API 統合と旧実装の置き換え

**Files:**
- Create: `src/logic/creatures/index.ts`
- Modify: `src/logic/creatureGenerator.ts`（全置換 → 再エクスポート）
- Test: 既存 `src/__tests__/logic/creatureGenerator.test.ts`（変更しない）

**Interfaces:**
- Consumes: Task 2〜6 の `generateSlime` / `generateBeast` / `generateBiped` / `generateEyeTentacle` / `generateOrb`
- Produces: 既存と同一の公開 API（`selectSpecies` / `generateCreatureName` / `generateCreature`）

- [ ] **Step 1: index.ts を実装**

```ts
// src/logic/creatures/index.ts
import type { KanjiDNA, CreatureSpec } from '../../types/game'
import { generateBiped } from './biped'
import { generateSlime } from './slime'
import { generateEyeTentacle } from './eyeTentacle'
import { generateBeast } from './beast'
import { generateOrb } from './orb'

// 種族順は旧実装と同一（0:Biped 1:Slime 2:EyeTentacle 3:Beast 4:Orb）
const GENERATORS: ((dna: KanjiDNA) => string)[] = [
  generateBiped,
  generateSlime,
  generateEyeTentacle,
  generateBeast,
  generateOrb,
]

export function selectSpecies(dna: KanjiDNA, word: string): number {
  const wordHash = word.split('').reduce((s, c) => s + (c.codePointAt(0) ?? 0), 0)
  return (dna.strokeCount + Math.round(dna.hRatio * 10) + wordHash) % 5
}

const SPECIES_NAME_FN: ((word: string) => string)[] = [
  (word) => `「${word}」マン`,
  (word) => `「${word}」ののろい`,
  (word) => `「${word}」アイ`,
  (word) => `「${word}」のけもの`,
  (word) => `そらとぶ「${word}」`,
]

export function generateCreatureName(species: number, word: string): string {
  return SPECIES_NAME_FN[species](word)
}

export function generateCreature(dna: KanjiDNA, word: string): CreatureSpec {
  const species = selectSpecies(dna, word)
  const svgString = GENERATORS[species](dna)
  const name = generateCreatureName(species, word)
  return { species, dna, svgString, name }
}
```

- [ ] **Step 2: creatureGenerator.ts を再エクスポートに置き換え（旧コード全削除）**

```ts
// src/logic/creatureGenerator.ts
// クリーチャー生成 v2（Issue #6）。実装は creatures/ 配下に分割。
// 設計: docs/superpowers/specs/2026-07-07-creature-v2-design.md
export { selectSpecies, generateCreatureName, generateCreature } from './creatures'
```

- [ ] **Step 3: 全テスト＋型チェック＋lint**

Run: `npx vitest run && npx tsc --noEmit && npx eslint src`
Expected: 既存 `creatureGenerator.test.ts` 含め全 PASS。型エラー 0。lint は新規エラーなし（既存 28 問題は対象外）

- [ ] **Step 4: 実アプリでの目視確認**

`npm run dev` でゲームを起動し、ステージを2〜3個開始して敵クリーチャーが
v2 デザイン（一筆シルエット・つややかな目・縁取り）で表示されることを確認。
`docs/superpowers/specs/2026-07-07-creature-v2-mockup.html` をブラウザで開いて見比べる。

- [ ] **Step 5: コミット**

```bash
git add src/logic/creatureGenerator.ts src/logic/creatures/index.ts
git commit -m "feat: switch creature generation to v2 part-composition system (#6)"
```

---

## 完了条件

- 全テスト PASS（既存 `creatureGenerator.test.ts` 無修正で PASS を含む）
- `npx tsc --noEmit` クリーン、lint 新規エラーなし
- 実アプリで5種族の v2 表示を目視確認（種族はステージの単語で変わる）
- マージは superpowers:finishing-a-development-branch に従い、ユーザー確認後に
  `--no-ff` で main へ。Issue #6 はユーザーの実機確認後にクローズ
